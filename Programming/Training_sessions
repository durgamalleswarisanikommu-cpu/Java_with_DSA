#1.Program using bitwise operator
import java.util.*;

public class day2
{
    public static void main(String args[])
    {
        Scanner sc=new Scanner(System.in);
        System.out.println("enter a value:\n");
        int a=sc.nextInt();
        System.out.println("enter b value:\n");
        int b=sc.nextInt();
        boolean opposite=((a^b)<0);
        System.out.println(opposite);
    }
}
output:
enter a value:8
enter b value:9
false

#2.program to print values
import java.util.*;
public class day2
{
    public static void main(String args[])
    {
        Scanner sc=new Scanner(System.in);
        System.out.println("enter a value:\n");
        int a=sc.nextInt();
        for(int i=0;i<=a;i++)
        {
          System.out.print(i+" ");
        }
    }
}

#3.Factorail of given number
import java.util.*;

public class day2
{
    public static void main(String args[])
    {
        Scanner sc=new Scanner(System.in);
        System.out.println("enter a value:\n");
        int a=sc.nextInt();
        int fact=1;
        for(int i=1;i<=a;i++)
        {
          fact=fact*i;
        }
        System.out.println("Factorial of a number is:" +fact);
    }
}

#4.Spy number : A Spy number is a number where the sum of digits = product of digits.  Ex:123

import java.util.*;

public class Main {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);

        int num = sc.nextInt();
        int temp = num;

        int sum = 0;
        int product = 1;

        while(temp > 0) {
            int digit = temp % 10;

            sum = sum + digit;
            product = product * digit;

            temp = temp / 10;
        }

        if(sum == product) {
            System.out.println("Spy number");
        }
        else {
            System.out.println("Not a Spy number");
        }

        sc.close();
    }
}

#5.import java.util.*;

public class Main {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);

        int n = sc.nextInt();

        int square = n * n;
        int temp = n;
        int count = 0;

        while(temp > 0) {
            count++;
            temp = temp / 10;
        }

        int last = square % (int)Math.pow(10, count);

        if(last == n) {
            System.out.println("Automorphic number");
        }
        else {
            System.out.println("Not an Automorphic number");
        }

        sc.close();
    }
}

#6.Magic Number : A Magic Number is a number where:

Repeatedly sum its digits until you get a single digit.
👉 If the final digit is 1, it is a Magic Number.

import java.util.*;

class Solution {
    public String isMagic(int n) {
        
        while (n > 9) {
            int sum = 0;
            
            while (n > 0) {
                sum += n % 10;
                n = n / 10;
            }
            
            n = sum;
        }
        
        if (n == 1) {
            return "Magic Number";
        } else {
            return "Not a Magic Number";
        }
    }
}

#7.Happy Number:

import java.util.*;
class Main {
    public static void main(String args[]) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();

        HashSet<Integer> set = new HashSet<>();

        while (n != 1 && !set.contains(n)) {
            set.add(n);

            int sum = 0;

            while (n > 0) {
                int digit = n % 10;
                sum += digit * digit;
                n = n / 10;
            }

            n = sum;
        }

        if (n == 1) {
            System.out.println("Happy Number");
        } else {
            System.out.println("Not a Happy Number");
        }
    }
}

#8.Perfect Number : A Perfect Number is a number where:
The sum of its proper divisors (excluding the number itself) is equal to the number.

import java.util.*;
class Solution {
    public static void main(String args[]) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();

        int sum = 0;

        for (int i = 1; i <= n / 2; i++) {
            if (n % i == 0) {
                sum += i;
            }
        }

        if (sum == n) {
            System.out.println("Perfect Number");
        } else {
            System.out.println("Not a Perfect Number");
        }
    }
}

#9.Fibonacci Series:
import java.util.*;

class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        System.out.print("Enter number of terms: ");
        int n = sc.nextInt();

        int a = 0, b = 1;

        for (int i = 1; i <= n; i++) {
            System.out.print(a + " ");

            int c = a + b;
            a = b;
            b = c;
        }
    }
}

#10.Nelson Number
import java.util.*;
class Main
  {
    public static void main(String args[])
    {
      Scanner sc=new Scanner(System.in);
      System.out.println("enter number:\n");
      int n=sc.nextInt();

      if( n >=111 && n <=999 && (n%111) == 0)
      {
        System.out.println("Nelson number");
      }
      else
      {
        System.out.println("Not aNelson number");
      }
    }
  
}

#11.Swap Two Numbers Without Using Temporary Variable (Java)
Method 1: Using Addition and Subtraction
import java.util.*;
class Main {
    public static void main(String args[]) {

        Scanner sc = new Scanner(System.in);

        int a = sc.nextInt();
        int b = sc.nextInt();

        System.out.println("Before swapping:");
        System.out.println("a = " + a);
        System.out.println("b = " + b);

        // swapping without temp variable
        a = a + b;
        b = a - b;
        a = a - b;

        System.out.println("After swapping:");
        System.out.println("a = " + a);
        System.out.println("b = " + b);
    }
}

#12.Reverse of a number
import java.util.*;

class Main {
    public static void main(String args[]) {

        Scanner sc = new Scanner(System.in);

        int n = sc.nextInt();
        int rev = 0;

        while (n > 0) {
            int digit = n % 10;     // get last digit
            rev = rev * 10 + digit; // add digit to reverse
            n = n / 10;             // remove last digit
        }

        System.out.println(rev);
    }
}

#13.Sum of digits
import java.util.*;
class Main {
    public static void main(String args[]) {

        Scanner sc = new Scanner(System.in);

        int n = sc.nextInt();
        int sum = 0;

        while (n > 0) {
            int digit = n % 10;  // get last digit
            sum = sum + digit;   // add digit
            n = n / 10;          // remove last digit
        }

        System.out.println(sum);
    }
}

#14.Perfect Square

import java.util.*;
class Main {
    public static void main(String args[]) {

        Scanner sc = new Scanner(System.in);

        int n = sc.nextInt();

        int sqrt = (int)Math.sqrt(n);

        if (sqrt * sqrt == n) {
            System.out.println("Perfect Square");
        } else {
            System.out.println("Not a Perfect Square");
        }
    }
}
